# SmartLedger Production Environment Configuration

# Custom domain or canonical URL for production (e.g., https://smartledger.rw)
# If left blank, the app dynamically uses window.location.origin
VITE_PUBLIC_APP_URL=https://smartledger.rw

# Optional: Gemini API Key for server-side AI Business Companion
GEMINI_API_KEY=
